import type { StyleSpecification } from "maplibre-gl";
/**
 * Default raster basemap for Storybook and consumers that omit `mapStyle`.
 */
export declare const DEFAULT_GEOJSON_MAP_STYLE: StyleSpecification;
export declare const DEFAULT_GEOJSON_MAP_VIEW_STATE: {
    longitude: number;
    latitude: number;
    zoom: number;
};
